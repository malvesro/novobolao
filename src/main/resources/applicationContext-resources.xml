<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/jdbc
           http://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

	<!-- Datasource configuration (alternatively you can use the JNDI way) -->
	<bean id="defaultDataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
		<property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
		<!--
		  Timezone explícito na sessão JDBC:
		  - os dados de jogos (data/hora) já são cadastrados no referencial São Paulo;
		  - em produção (HF), host/container pode operar com outro fuso.
		  Assim evitamos drift em colunas TIMESTAMP/CURRENT_TIMESTAMP.
		-->
		<property name="jdbcUrl" value="jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:bolao}?useSSL=true&amp;requireSSL=true&amp;verifyServerCertificate=false&amp;allowPublicKeyRetrieval=true&amp;useUnicode=true&amp;characterEncoding=UTF-8&amp;connectionCollation=utf8mb4_unicode_ci&amp;cachePrepStmts=true&amp;prepStmtCacheSize=250&amp;prepStmtCacheSqlLimit=2048&amp;useServerPrepStmts=true&amp;connectionTimeZone=America/Sao_Paulo&amp;forceConnectionTimeZoneToSession=true" />
		<property name="username" value="${DB_USER:root}" />
		<property name="password" value="${DB_PASSWORD}" />
		<property name="maximumPoolSize" value="10" />
		<property name="minimumIdle" value="5" />
		<property name="idleTimeout" value="30000" />
		<property name="connectionTimeout" value="30000" />
		<property name="maxLifetime" value="1800000" />
		<property name="keepaliveTime" value="300000" />
		<property name="initializationFailTimeout" value="0" />
		<property name="leakDetectionThreshold" value="60000" />
	</bean>
	
	<!--
	  Inicializacao de banco com hardening para producao:
	  - DB_INIT_SCHEMA: aplica apenas estrutura (idempotente).
	  - DB_INIT_SEED: carrega seed base.
	  - DB_INIT_COPA_2026_SEED: carrega seed de jogos/palpites (pode conter reset de dados).
	  Compatibilidade retroativa:
	  - DB_INITIALIZE antigo segue suportado apenas para schema.
	-->
	<jdbc:initialize-database data-source="defaultDataSource" enabled="${DB_INIT_SCHEMA:${DB_INITIALIZE:true}}">
		<jdbc:script location="classpath:database/schema.sql" />
	</jdbc:initialize-database>

	<jdbc:initialize-database data-source="defaultDataSource" enabled="${DB_INIT_SEED:false}">
		<jdbc:script location="classpath:database/data.sql" />
	</jdbc:initialize-database>

	<jdbc:initialize-database data-source="defaultDataSource" enabled="${DB_INIT_COPA_2026_SEED:false}">
		<jdbc:script location="classpath:database/copa-2026-data.sql" encoding="UTF-8" />
	</jdbc:initialize-database>

</beans>
